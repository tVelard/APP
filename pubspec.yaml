name: track_training
description: Application iOS de tracking d'entraînement avec authentification Supabase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase pour l'authentification
  supabase_flutter: ^2.5.0

  # Navigation
  go_router: ^14.0.0

  # State management
  flutter_riverpod: ^2.5.0

  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
